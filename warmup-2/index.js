import { countXX } from "./countXX";
import { frontTimes } from "./frontTimes";

export const Warmup2 = {
    stringTimes: (str, n) => stringTimes(str, n),
    frontTimes: (str, n) => frontTimes(str, n),
    countXX: (str) => countXX(str),
}